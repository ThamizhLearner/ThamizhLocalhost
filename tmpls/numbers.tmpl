{{define "ActivityPanel"}}
   <h2>Numbers</h2>
   <p style="font-weight:bold;font-family:courier;color:darkorange">[ Status: Work in progress ]</p>

   {{template "SimpleTable" .NumTable}}

{{end}}

{{define "SimpleTable"}}
<div>
   <h4>{{.Title}}</h4>
   <table>
      <tr>
      {{range .ColInfoList}}
         <th colspan="{{.Span}}">{{.Header}}</th>
      {{end}}
      </tr>

      {{range .Cells}}
         <tr>
         {{range .}}
            <td>
            {{.}}
            </td>
         {{end}}
         </tr>
      {{end}}
   </table>
</div>
{{end}}
